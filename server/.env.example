# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# CORS - Allowed Origins (REQUIRED for login to work)
# Add your production domain and localhost for development
ALLOWED_ORIGIN=https://your-domain.github.io
ALLOWED_ORIGINS=https://your-domain.github.io,http://localhost:3000,http://localhost:5500

# Security - CSRF shared secret (generate a random string for production)
CSRF_SHARED_SECRET=generate-a-random-secret-here

# Server Configuration
PORT=10000

# Optional: GitHub Configuration (if not using Supabase)
# GITHUB_TOKEN=your-github-token
# REPO_OWNER=your-github-username
# REPO_NAME=EventCall-Data
